# Porta do servidor (padrão: 3000)
PORT=3000

# Diretório do banco de dados (padrão: ./data)
DB_DIRECTORY=./data

# Chave secreta para as sessões
SESSION_SECRET=sua_chave_secreta_aqui

# Tempo de expiração da sessão em milissegundos (padrão: 24 horas)
SESSION_EXPIRY=86400000
